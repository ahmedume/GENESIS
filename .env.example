# GENESIS.EXE — placeholder file only.
# This project intentionally has NO secrets: it is a static client-only site.
# Never commit real keys/tokens/emails. Enforced by QA-SECURITY.md S1–S2.
NODE_ENV=development
