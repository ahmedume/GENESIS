# deps & builds
node_modules/
dist/
dist-ssr/

# vite transient files (config-error dumps)
vite.config.*.timestamp-*

# env & secrets — NEVER commit real values
.env
.env.*
!.env.example

# logs & OS junk
*.log
.DS_Store
Thumbs.db
Desktop.ini

# editor
.idea/
.vscode/

# AI tooling artifacts — deliberately removed from repo, keep them out
.codex/
